Run `./run.sh` to start a training process. You should prepare the data under ./dataset directory. ./dataset directory should be like this:

```shell
.
├── ribfrac-test
│   └── images
├── ribfrac-train
│   ├── images
│   └── labels
└── ribfrac-val
    ├── images
    └── labels
```

